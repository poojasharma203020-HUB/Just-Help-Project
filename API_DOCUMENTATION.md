# 📚 Complete API Documentation\n\n## 🔑 Authentication\n\n### Sign Up\n```typescript\nconst { data, error } = await supabase.auth.signUp({\n  email: 'user@example.com',\n  password: 'SecurePassword123!'\n});\n```\n\n### Login\n```typescript\nconst { data, error } = await supabase.auth.signInWithPassword({\n  email: 'user@example.com',\n  password: 'SecurePassword123!'\n});\n```\n\n### Logout\n```typescript\nconst { error } = await supabase.auth.signOut();\n```\n\n### Get Current User\n```typescript\nconst { data: { user } } = await supabase.auth.getUser();\n```\n\n---\n\n## 👥 Users API\n\n### Create User Profile\n```typescript\nconst { data, error } = await supabase\n  .from('users')\n  .insert([\n    {\n      auth_id: user.id,\n      email: user.email,\n      first_name: 'John',\n      last_name: 'Doe',\n      user_type: 'customer', // customer, provider, vendor, delivery\n      phone: '+1234567890'\n    }\n  ])\n  .select();\n```\n\n### Get User Profile\n```typescript\nconst { data, error } = await supabase\n  .from('users')\n  .select('*')\n  .eq('id', userId)\n  .single();\n```\n\n### Update User Profile\n```typescript\nconst { data, error } = await supabase\n  .from('users')\n  .update({\n    first_name: 'Jane',\n    avatar_url: 'https://...',\n    phone: '+9876543210'\n  })\n  .eq('id', userId)\n  .select();\n```\n\n---\n\n## 🛍️ Orders API\n\n### Create Order\n```typescript\nconst { data, error } = await supabase\n  .from('orders')\n  .insert([\n    {\n      order_number: 'ORD-001',\n      customer_id: customerId,\n      provider_id: providerId,\n      service_id: serviceId,\n      scheduled_date: '2026-08-15T10:00:00',\n      order_value: 150.00,\n      tax: 15.00,\n      discount: 10.00,\n      total: 155.00,\n      notes: 'Please arrive 10 minutes early'\n    }\n  ])\n  .select();\n```\n\n### Get Orders\n```typescript\n// Get all orders for customer\nconst { data, error } = await supabase\n  .from('orders')\n  .select('*')\n  .eq('customer_id', customerId)\n  .order('created_at', { ascending: false });\n\n// Get active orders\nconst { data, error } = await supabase\n  .from('orders')\n  .select('*')\n  .in('status', ['pending', 'confirmed', 'in_progress'])\n  .order('created_at', { ascending: false });\n```\n\n### Update Order Status\n```typescript\nconst { data, error } = await supabase\n  .from('orders')\n  .update({ status: 'in_progress' })\n  .eq('id', orderId)\n  .select();\n```\n\n### Cancel Order\n```typescript\nconst { data, error } = await supabase\n  .from('orders')\n  .update({\n    status: 'cancelled',\n    payment_status: 'refunded'\n  })\n  .eq('id', orderId)\n  .select();\n```\n\n---\n\n## 💳 Payments API\n\n### Process Payment\n```typescript\nconst { data, error } = await supabase\n  .from('payments')\n  .insert([\n    {\n      order_id: orderId,\n      amount: orderTotal,\n      payment_method: 'credit_card', // credit_card, debit_card, upi, wallet\n      status: 'pending',\n      payment_gateway: 'stripe',\n      payer_email: userEmail\n    }\n  ])\n  .select();\n```\n\n### Get Payment Status\n```typescript\nconst { data, error } = await supabase\n  .from('payments')\n  .select('*')\n  .eq('order_id', orderId)\n  .single();\n```\n\n### Refund Payment\n```typescript\nconst { data, error } = await supabase\n  .from('payments')\n  .update({ status: 'refunded' })\n  .eq('id', paymentId)\n  .select();\n```\n\n---\n\n## ⭐ Ratings API\n\n### Create Rating\n```typescript\nconst { data, error } = await supabase\n  .from('ratings')\n  .insert([\n    {\n      order_id: orderId,\n      rater_id: customerId,\n      ratee_id: providerId,\n      rating: 5,\n      review: 'Excellent service!',\n      professionalism: 5,\n      punctuality: 5,\n      communication: 5,\n      quality: 5\n    }\n  ])\n  .select();\n```\n\n### Get Ratings for Provider\n```typescript\nconst { data, error } = await supabase\n  .from('ratings')\n  .select('*')\n  .eq('ratee_id', providerId);\n```\n\n### Get Average Rating\n```typescript\nconst { data, error } = await supabase\n  .from('ratings')\n  .select('rating')\n  .eq('ratee_id', providerId);\n\nconst averageRating = data.reduce((sum, r) => sum + r.rating, 0) / data.length;\n```\n\n---\n\n## 🚚 Delivery API\n\n### Assign Delivery\n```typescript\nconst { data, error } = await supabase\n  .from('delivery_assignments')\n  .insert([\n    {\n      order_id: orderId,\n      delivery_partner_id: partnerId,\n      status: 'assigned'\n    }\n  ])\n  .select();\n```\n\n### Update Delivery Location\n```typescript\nconst { data, error } = await supabase\n  .from('delivery_partners')\n  .update({\n    current_latitude: 40.7128,\n    current_longitude: -74.0060\n  })\n  .eq('id', partnerId)\n  .select();\n```\n\n### Get Real-time Delivery Updates\n```typescript\nsupabase\n  .from('delivery_partners')\n  .on('*', payload => {\n    console.log('Delivery update:', payload);\n  })\n  .subscribe();\n```\n\n---\n\n## 💬 Chat API\n\n### Send Message\n```typescript\nconst { data, error } = await supabase\n  .from('messages')\n  .insert([\n    {\n      chat_room_id: chatRoomId,\n      sender_id: userId,\n      message_text: 'Hello! I have a question.'\n    }\n  ])\n  .select();\n```\n\n### Get Chat History\n```typescript\nconst { data, error } = await supabase\n  .from('messages')\n  .select('*')\n  .eq('chat_room_id', chatRoomId)\n  .order('created_at', { ascending: true });\n```\n\n### Real-time Chat\n```typescript\nsupabase\n  .from('messages')\n  .on('INSERT', payload => {\n    console.log('New message:', payload.new);\n  })\n  .subscribe();\n```\n\n---\n\n## 🔔 Notifications API\n\n### Create Notification\n```typescript\nconst { data, error } = await supabase\n  .from('notifications')\n  .insert([\n    {\n      user_id: userId,\n      title: 'Order Confirmed',\n      body: 'Your order ORD-001 has been confirmed',\n      type: 'order_confirmed',\n      related_id: orderId\n    }\n  ])\n  .select();\n```\n\n### Get Notifications\n```typescript\nconst { data, error } = await supabase\n  .from('notifications')\n  .select('*')\n  .eq('user_id', userId)\n  .eq('is_read', false)\n  .order('created_at', { ascending: false });\n```\n\n### Mark as Read\n```typescript\nconst { data, error } = await supabase\n  .from('notifications')\n  .update({\n    is_read: true,\n    read_at: new Date().toISOString()\n  })\n  .eq('id', notificationId)\n  .select();\n```\n\n---\n\n## 📊 Analytics API\n\n### Get Dashboard Stats\n```typescript\n// Total Orders\nconst { data: orders } = await supabase\n  .from('orders')\n  .select('id');\nconst totalOrders = orders?.length || 0;\n\n// Total Revenue\nconst { data: payments } = await supabase\n  .from('payments')\n  .select('amount')\n  .eq('status', 'success');\nconst totalRevenue = payments?.reduce((sum, p) => sum + p.amount, 0) || 0;\n\n// Active Users\nconst { data: users } = await supabase\n  .from('users')\n  .select('id')\n  .eq('status', 'active');\nconst activeUsers = users?.length || 0;\n```\n\n### Get Orders by Date Range\n```typescript\nconst { data, error } = await supabase\n  .from('orders')\n  .select('*')\n  .gte('created_at', startDate)\n  .lte('created_at', endDate)\n  .order('created_at', { ascending: true });\n```\n\n### Get Provider Performance\n```typescript\nconst { data, error } = await supabase\n  .from('providers')\n  .select('*, ratings(rating)')\n  .eq('id', providerId)\n  .single();\n```\n\n---\n\n## 🔐 Security & RLS\n\n### Row Level Security is enabled on all tables\n\n```sql\n-- Users can only see their own data\nCREATE POLICY \"Users can view their own profile\"\n  ON users FOR SELECT\n  USING (auth.uid() = auth_id);\n\n-- Admins can see everything\nCREATE POLICY \"Admins can view all data\"\n  ON users FOR SELECT\n  USING (auth.jwt() ->> 'role' = 'admin');\n```\n\n---\n\n## 🆘 Error Handling\n\n```typescript\ntry {\n  const { data, error } = await supabase\n    .from('orders')\n    .select('*')\n    .eq('id', orderId);\n\n  if (error) {\n    console.error('Error fetching order:', error.message);\n    throw new Error(error.message);\n  }\n\n  return data;\n} catch (error) {\n  console.error('Failed to fetch order:', error);\n  // Handle error gracefully\n  return null;\n}\n```\n\n---\n\n## 📡 Real-time Subscriptions\n\n```typescript\n// Subscribe to order updates\nconst subscription = supabase\n  .from('orders')\n  .on('*', payload => {\n    if (payload.eventType === 'UPDATE') {\n      console.log('Order updated:', payload.new);\n    }\n  })\n  .subscribe();\n\n// Cleanup\nreturn () => subscription.unsubscribe();\n```\n\n---\n\n## 🚀 Best Practices\n\n1. **Always use error handling**\n2. **Validate user input** before sending to database\n3. **Use RLS policies** for security\n4. **Cache responses** when appropriate\n5. **Use pagination** for large datasets\n6. **Subscribe to real-time updates** for live data\n7. **Log all errors** for debugging\n8. **Test all API calls** before deployment\n\n---\n\n## 📖 Full Documentation\n\n- [Supabase Docs](https://supabase.com/docs)\n- [PostgreSQL Docs](https://www.postgresql.org/docs)\n- [React Query](https://tanstack.com/query/latest)\n- [Flutter Supabase](https://supabase.com/docs/reference/dart/intro)\n"